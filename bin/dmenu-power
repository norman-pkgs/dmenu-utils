#!/bin/zsh

powers="shutdown
blank
reboot
win10"

choice=$(echo $powers | dmenu)

[[ $choice == "shutdown" ]] && shutdown now
[[ $choice == "blank" ]] && blank
[[ $choice == "reboot" ]] && reboot
[[ $choice == "win10" ]] && dmenu-virtualmachine
